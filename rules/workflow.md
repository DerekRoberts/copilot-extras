# Workflow Rules

A task is NOT complete until all code changes are committed and pushed to the remote branch. After changes: `git add`, `git commit` (conventional format), `git push`, then `git status` must show clean.
